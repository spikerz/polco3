- model_class = LegislatorVote

- title 'Legislator Votes'

= paginate @legislator_votes

%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th Value
      %th Legislator
      %th Roll
      %th Actions
  %tbody
    - @legislator_votes.each do |legislator_vote|
      %tr
        %td= link_to legislator_vote.id, legislator_vote_path(legislator_vote)
        %td= legislator_vote.value
        %td= link_to legislator_vote.legislator.full_name, legislator_path(legislator_vote.legislator)
        %td= link_to  legislator_vote.roll.title, roll_path(legislator_vote.roll)
        %td
          = link_to 'Edit', edit_legislator_vote_path(legislator_vote), :class => 'btn btn-mini'
          = link_to 'Destroy', legislator_vote_path(legislator_vote), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to 'New', new_legislator_vote_path, :class => 'btn btn-primary'

= paginate @legislator_votes
