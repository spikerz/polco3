= simple_form_for @legislator_vote, :html => { :class => 'form-horizontal' } do |f|
  = f.input :value
  = f.input :legislator_id
  = f.input :roll_id
  = f.input :bill_id
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to 'cancel', legislator_votes_path, :class => 'btn'
